/** Publish testimonials only after source attribution and consent are documented. */
export const verifiedTrustContent: { testimonials: boolean; videoTestimonials: boolean } = {
  testimonials: false,
  videoTestimonials: false,
};
